# 文档编写规则

- 先确认范围：只修改当前任务直接涉及的文档；发现无关问题先报告，不顺手修正。
- 优先维护唯一事实源，避免复制同一结论；新增文档先说明读者、目的和下一步。
- 以代码、配置、测试和迁移为准，明确区分 `已实现`、`部分落地`、`未实现`、`待验证`。
- 接口、状态、配置、版本和路径必须使用准确标识；修改后检查示例、链接、标题和代码块。
- Fork 专属或 VOS 专属内容放在 `ictrek.app/`；上游文档仅在明确准备贡献上游时修改；`docs/ictrek/` 不新增内容。
- 无关的索引清理、过时内容修正或其他功能补充另开任务，或先获得确认。
- 完成前运行 `git diff --check`，并报告改动、验证结果和未决事项。
