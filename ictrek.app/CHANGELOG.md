# VOS HybRAG Changelog

记录 VOS HybRAG 应用的可验证变化。

## [Unreleased]

### 新增

- **统一工具箱入口** — 侧栏新增工具箱，集中管理 Skills、MCP 服务和 BrowserSkill 连接；入口继续遵循角色与部署能力限制，原设置页书签会跳转到新位置。
- **单次对话推理等级** — 支持的模型可在输入栏为当前对话选择推理等级；选择随会话请求传递，不会改写全局设置或已保存的 Agent 配置，恢复默认后继续继承 Agent 配置。
- **资源排序与 IM 回复语言** — 知识库、文档、Agent 和组织列表可按更新时间、创建时间或名称排序；IM 渠道可固定使用简体中文、英语、日语、韩语或俄语回复，留空时继续使用部署默认语言。

### 优化

- **BrowserSkill 安装、授权与任务状态** — app 镜像中的 daemon 和浏览器扩展统一使用上游 `0.3.1` 源码；安装页增加 Chrome、Edge 商店入口和旧版本提示，任务预览可显示页面、状态并执行暂停、恢复、停止和定位。由 Agent Window 原生操作打开的标签页可直接受控，其他标签页仍需浏览器确认授权。
- **MCP 连接与结果可用性** — 前端网关支持 `/mcp/` 长连接转发；修复 endpoint 会话标识不一致导致的 `session not found`，Wiki 工具向结构化结果返回正文，文本与结构化结果中的图片在 endpoint 范围和知识库权限校验后转换为可访问链接。
- **知识处理诊断与恢复** — 文档处理界面可区分排队和长时间无进展，并显示最近活动时间、停滞阶段和取消入口；后端将任务交接、状态读取、处理步骤和 panic 收敛为可重试或明确失败，失败前未执行的阶段不再误标为失败。Wiki 任务取消、finalizing 槽释放和丢失任务恢复同步完善。
- **知识图谱检索边界** — Neo4j 检索优先展开精确名称和较短名称，单次最多使用 200 个种子节点并返回 2,000 行；达到行数上限时记录截断警告，避免模糊实体名称一次拉取整个邻域。结果被截断时需要缩小实体范围。
- **聊天时间与输入栏显示** — 较早消息显示完整日期；聊天输入栏在消息区域滚动或回弹时保持固定，知识引用浮层与问题大纲卡片保持对齐。
- **API 文档来源对齐** — 前端镜像同时打包 VOS API 索引和上游维护的 `website-docs/04-api/` 内容，设置页可在两类文档间解析内部链接，避免文档迁移后入口指向失效页面。

### 修复

- **共享知识库图片读取** — 解析图片会绑定所属文档，共享知识库检索可按资源权限读取父文本块和图片信息；既有历史文档需要重新处理后才能补齐绑定关系。
- **Wiki 页面与队列稳定性** — 实体类型变化时优先复用同名页面，减少重复页和交叉链接；页面重写遇到输出长度限制时最多续写三轮，仍被截断则拒绝保存不完整内容。软删除租户的待处理 Wiki 队列会被清理，不再继续调用模型。
- **文档转换与上传清理** — 非 ASCII 的无效 base64 图片会被跳过而不是中断整份文档；转换超时会终止整个子进程组，上传请求结束后清理 multipart 临时文件，减少 worker 残留进程和容器临时文件堆积。
- **GitLab 数据源同步** — 项目列表读取全部分页；增量 compare 请求失败或超时时明确终止并返回错误，不再静默退化为全范围枚举。
- **技能必填环境变量识别** — 沙箱创建配置中的 `env_vars` 现在可以满足 Skill 的必填变量检查，不再把已经写入容器的变量误报为缺失；这些值只参与检查，不会在每次执行时重复注入。
- **BrowserSkill 调用参数校验** — `local_browser` 按方法声明允许的参数，并统一使用 `max_text_chars` 限制文本；页面读取会将该限制换算为扩展的 token 上限，避免 Agent 发送不适用字段或混用两套文本限制。
- **QA 非法输入提前拒绝** — QA 请求在查找会话或启动处理前校验输入语法，无效内容直接返回 `400`；Agent 对话仍允许正常的前端代码文本。

## [0.1.63] - 2026-09-23

### 修复

- **VOS 语言环境跟随已部署 VIVIBIT** — VOS 应用可识别 `VIVIBIT-*-preferences-locale` 本地存储键，并继续兼容 `vben-web-antd-*-preferences-locale`。自动跟随仅接受 `zh-CN`、`en-US`、`ru-RU`、`ko-KR`、`ja-JP`；用户设置 `weknora-locale-mode=manual` 后不再响应外部 VOS 语言变更。

## [0.1.62] - 2026-09-23

### 修复

- **VOS 平台语言同步** — VOS 页面读取并监听带命名空间的 `vben-web-antd-*-preferences-locale` 存储键，同时兼容原有未命名空间的 `preferences-locale`；`/app/com.ictrek.hybrag/` 页面可按平台语言初始化并响应跨页语言变更，手动语言模式保持不跟随。

## [0.1.61] - 2026-09-23

### 打包与部署

- **前端镜像内置 API 文档** — 前端 Docker 构建上下文改为仓库根目录 `.`，将 `docs/api/README.md` 和 `docs/api/vos-external-api.md` 复制到构建阶段供 Vite 打包；构建时校验两个文件非空，缺失则直接失败，避免 VOS Web 镜像中的 API 文档页面为空。

## [0.1.60] - 2026-09-23

### 重点变化

- **统一 Agent 知识检索入口** — 将重叠的检索工具收敛为 `search_knowledge`、`read_document` 和 `list_documents`；支持 `hybrid`、`semantic`、`keyword` 模式、文档内关键词查询、分页和上下文读取，并保留旧工具名的运行时兼容映射。绑定知识库会在回答前先检索，建议问题会携带并校验来源文档。
- **智能档案解析与知识库镜像解耦** — 解析、OCR 和托管知识库镜像分别持久化状态；档案可在解析结果保存后展示，镜像失败只重试镜像任务，页面显示阶段进度，取消、重启恢复和并发清理路径均有明确状态。托管镜像知识库保持只读，相关操作通过智能档案入口完成。详见[智能档案文档](docs/smart-archive.md)。
- **VOS Model Hub 与原生 Ollama 运行** — 默认 QA/VLM、embedding 和 rerank 端点由 Gateway `11535` 切换到原生 Ollama `11434`；缺失模型通过 Model Hub 管理接口下载，设置页显示下载进度，并同步更新默认模型、预热与等待地址。

### 新增

- **产物库与生成文件删除** — 新增 `GET /api/v1/artifacts` 产物库，按会话和源路径展示文件最新版本，支持关键词、文件类型筛选、分页、预览、下载和跳转会话；仅 web 会话进入该列表。会话内和产物库均可删除生成文件，删除采用 `deleted_at` 软删除并按资源引用计数回收字节，保留旧链接定位和并发写入安全。涉及 PostgreSQL `000103`、SQLite `000023` 产物表及 PostgreSQL `000125`、SQLite `000032` 删除标记迁移。
- **知识库文件上传任务面板** — 批量上传改为全局队列，提供整体进度、字节数、预计剩余时间、解析状态、取消、重试、问题筛选和文档入口；上传后会继续跟踪解析完成状态，并在页面离开前提示仍在运行的任务。默认并发为 3 个文件，同一知识库的同大小文件不会并行重复提交；未显式设置超时时，上传超时会按请求体大小计算。
- **会话原地回滚** — 新增 `POST /sessions/:id/rewind`，可回滚到用户或助手消息并截断其后的会话内容；存在可达检查点时同步重置沙箱工作区。发送中、无检查点或工作区已变化时返回冲突并保持会话数据，回滚与发送互斥，成功后重新加载历史并在用户消息处预填原问题。
- **已保存答案 Markdown 编辑器** — 保存答案时自动移除内部 `<kb>` 引用标签、集中展示引用文档并保留 web 链接；编辑器支持编辑/分栏/预览、全屏、常用 Markdown 快捷键、列表续写、未保存确认和字符/行数统计。
- **VOS API 文档入口** — 设置页新增面向普通空间成员的 API 文档导航和 Markdown 浏览组件；`0.1.60` 前端镜像尚未包含仓库级 `docs/api/` 内容，部署后页面可能为空，该打包问题已在 `0.1.61` 修复。
- **VOS 语言设置** — VOS 路径下新增自动跟随平台语言和手动选择模式；本版本自动模式读取同源未命名空间的 `preferences-locale`，后续版本补充了实际部署使用的命名空间键。

### 优化

- **Agent 历史与上下文压缩** — 压缩摘要持久化到消息的 `context_checkpoint`，后续轮次和分叉会从最近检查点继续；Agent 历史按模型窗口和校准后的 token 估算加载，快速问答仍保留原有轮数上限。
- **流式回答恢复** — 重放已保存的回答流时合并同一片段的连续事件，减少刷新或重新进入会话时的事件数量；新一轮回答不再继承上一轮文本。工具执行失败按 `response_type=tool_result` 返回，使 Agent 可以恢复；达到 completion 上限时结束当前轮次，并在 SSE、历史重建和界面中保留截断标记。
- **聊天、知识库和资源界面** — 优化知识库文档浏览、标签编辑、聊天面板间距、滚动、问题大纲、引用和产物展示；统一资源图标、共享筛选状态和会话分叉入口，并恢复聊天导航与引用初始化。
- **解析时间线** — 失败卡片优先展示具体阶段和后端错误，重试入口靠近失败原因；时间轴刻度和偏移保持在图表范围内，失败后未执行的阶段标记为未运行。
- **知识处理稳定性** — 所有解析器在分块前统一规范内联 HTML 表格，并限制 Markdown 链接/图片保护范围，避免超长或跨段保护区域阻塞分块；嵌入模型解析失败和多模态任务入队缺口会落盘为明确失败，不再长期停留在 `processing`；任务队列优先处理失败次数较少的任务；临时文件写入失败会清理文件；Qdrant 中不存在目标维度集合时删除向量按空集合处理。
- **Wiki 与知识图谱结果** — 去重提示同时比较名称和显式别名并拒绝仅凭歧义缩写合并；跳过不明确的单汉字自动链接，扩大实体抽取输出预算；缺失历史快照时以可用内容降级展示；Reduce 阶段引用的文本块会带入启用的图片描述/OCR 信息。
- **对象存储传输边界** — S3、COS、KS3、OBS、OSS 等对象传输不再受 30 秒整请求超时限制，连接建立、响应头、探测和后台任务分别设置边界；KS3 保留调用方上下文并仅在真实 404 时创建桶。存储路径规范化不再把后端作用域误当作对象路径的一部分。
- **IMA 与 BrowserSkill 稳定性** — IMA 子目录遍历使用列表响应中的媒体 ID；BrowserSkill 重新对齐补丁并使用原生会话生命周期和导航响应截止时间。
- **技能版本升级** — 过期技能可从技能目录、沙箱技能面板和 Agent 编辑器升级；升级过程中继续提供上一个已就绪版本，升级失败或停止时不将技能下线，也不会用重试操作回写目录定义。

### 修复

- **模型调用和文档解析错误处理** — 远程嵌入服务重试耗尽后返回真实错误而不是 `(nil, nil)`，DashScope 多模态向量按响应中的 `index` 写回批次位置；知识库自定义指令不再污染 OCR 提示词；PDF 默认继续使用内置解析器；聊天模型错误不会丢失 Ollama 代理配置。
- **建议问题去除当前问题** — 模型生成的建议和知识库候选均会按规范化文本过滤与当前问题完全相同的内容；知识库候选可由后续候选补位，模型生成的建议过滤后可能少于配置数量。
- **XMind 上传校验** — 前端附件和后端临时文档白名单加入 `.xmind`，文件在解析引擎发现前不再被误判为不支持。
- **共享工作区图片恢复** — 回答完成并持久化后，在权限可用时重试受保护图片加载，覆盖普通、快速时间线和 Agent 会话，避免流式结束后图片继续显示失败。
- **合同审查 PDF 证据定位** — 对同一位置的重复 PDF 文本去重后再匹配引用，避免视觉叠加文本使合同审查证据无法定位。
- **缩放状态下的操作引导定位** — 浏览器或根页面缩放时，高亮区域和引导浮层保持对齐。
- **会话标题失败降级** — 标题模型不可用、调用失败或返回空标题时，使用首条问题生成并持久化兜底标题，同时优先使用本轮实际对话模型。

### 打包与部署

- **VOS App Store 发布通道** — VOS 发布工作流以 `channel=stable` 发布应用包；遇到不接受该字段的旧接口时回退到无通道参数的请求。
- **迁移版本冲突** — 上下文检查点迁移固定为 PostgreSQL `000105`，智能档案镜像相关迁移调整为 PostgreSQL `000126`–`000129`、SQLite `000033`–`000036`，避免重复版本导致 `golang-migrate` 无法启动或升级。
- **开发环境健康检查** — `docker-compose.dev.yml` 的数据库健康检查改为连接配置中指定的数据库，而不是固定使用默认数据库名。
- **大陆环境构建与缓存复用** — APT 默认使用 Aliyun 镜像，BrowserSkill 下载增加备用源，Cargo 支持 RsProxy 回退；Docker 构建复用 npm、pnpm、Cargo 和 Go 缓存，并延后易变版本层，减少重复构建下载。

### 模型与运行

- **模型协议、目录与厂商能力统一** — 对话、embedding、rerank 和 ASR 请求按协议包、`internal/models/catalog` 和厂商声明分层，增加基于厂商文档的 wire/golden 校验并修正多家厂商能力事实。`extra_config.api` 作为协议选择器且非法值会被拒绝；未设置 `api_version` 的 Azure 行使用 `/openai/v1`，官方 `api.openai.com` 请求使用 Responses 协议；rerank 的分批、分数标度和截断按厂商能力执行，`qwen3-rerank` 的不匹配配置在构造阶段拒绝；ASR 目录覆盖文档已声明该能力的厂商。Wiki taxonomy embedding 和 WeKnoraCloud embedding 保留批处理及并发限制。

### 安全与权限

- **跨工作区共享边界** — 预签名预览、知识库初始化、模型/存储配置、Wiki 写工具、嵌入和 MCP 范围均按资源所属工作区和共享权限校验；共享知识库响应不再携带存储或 VLM 凭据，自己的 Agent 优先于同名共享 Agent，匿名嵌入请求不能自选知识库、模型或问题来源；只读共享知识库隐藏写操作。共享 Agent 的 Skill/MCP 选择器读取资源所属工作区，MCP 响应使用收窄字段；沙箱固定配置同时记录所属工作区，删除、终端、桌面和分叉路径可正确解析借用的沙箱。
- **KS3 重定向 SSRF 防护** — 恢复 KS3 客户端的安全重定向策略，重定向目标继续经过 SSRF 检查，并避免把请求签名带到跨主机目标。
- **API Key 操作审计** — 知识库活动记录保存并展示发起操作的 API Key 名称，同时不把异步任务重新授权为该 Key。

### 已知限制

- **数据库驱动范围** — 运行时的 `DB_DRIVER` 当前只接入 PostgreSQL 和 SQLite；仓库中的 `migrations/mysql/` 仅作未接入的参考脚本，不能据此声称已支持 MySQL。

[Unreleased]: https://github.com/ictrektech/WeKnora/compare/vos-hybrag-v0.1.63...HEAD
[0.1.63]: https://github.com/ictrektech/WeKnora/compare/vos-hybrag-v0.1.62...vos-hybrag-v0.1.63
[0.1.62]: https://github.com/ictrektech/WeKnora/compare/vos-hybrag-v0.1.61...vos-hybrag-v0.1.62
[0.1.61]: https://github.com/ictrektech/WeKnora/compare/vos-hybrag-v0.1.60...vos-hybrag-v0.1.61
[0.1.60]: https://github.com/ictrektech/WeKnora/compare/vos-hybrag-v0.1.58...vos-hybrag-v0.1.60
